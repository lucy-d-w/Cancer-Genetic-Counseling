import React from 'react';
import Quiz from '../Quiz';


class Quiz12 extends Quiz {
    render() {
        return (
            <Quiz question="It is more common to find variants of uncertain significance (VUS) as more genes are tested.">
            </Quiz>
        );
    }
}

export default Quiz12;